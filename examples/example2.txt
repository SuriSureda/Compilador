function printSquare(index : number) : void {
	square : number = index * index ;

	print(" NUMBER : ");
	print(index);
	print("\tSQUARE : ");
	print(square);
	print("\n");
}

function main() {
	index : number = 0;
	while((index < 100)){
		printSquare(index);
		index = index + 1;
	}
}